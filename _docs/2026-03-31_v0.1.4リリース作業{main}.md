# 2026-03-31 v0.1.4リリース作業{main}

## 実施内容
- SemVer 更新:
  - `Cargo.toml`: `hypura 0.1.3 -> 0.1.4`
  - `hypura-sys` は `0.1.3` を維持（FFI crate API変更なし）
- README リライト:
  - Release セクションを `0.1.4` に更新
  - Kobold-lite の GGUF モデル切替 UI/API 追加内容を追記
- リリースビルド:
  - `cargo build --release --target-dir target_release_rtx`
  - 既存 `hypura.exe` のロック回避のため `hypura_v013.exe` に退避後、差分ビルドで再生成
- 成果物作成:
  - `dist/hypura-rtx3060-3080-stable-v0.1.4.tar.gz`

## GHCLI リリース手順（実行）
- `v0.1.4` タグ作成 + release 作成 + 成果物添付
- `stable/v0.1.4` ブランチ作成
- `stable-v0.1.4` タグ作成 + stable release 作成 + 同一成果物添付

## 備考
- Windows 環境で既存 `target_release_rtx/release/hypura.exe` が使用中だと上書き失敗するため、退避して差分ビルド。
