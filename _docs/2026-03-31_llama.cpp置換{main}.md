# llama.cpp 置換実装ログ

- 実施日: 2026-03-31
- 対象: `vendor/llama.cpp` ビルド成果物でローカル `llama.cpp` 実行ファイルを置換
- 作業ディレクトリ: `c:\Users\downl\Desktop\hypura-main\hypura-main`

## 実施内容

1. `vendor/llama.cpp` のビルド設定を確認し、Windows + CUDA 環境で `llama-cli` / `llama-server` を生成。
2. 増分ビルド済みディレクトリ `vendor/llama.cpp/build-turboquant` で以下をビルド:
   - `llama-cli.exe`
   - `llama-server.exe`
3. 既存インストール先:
   - `C:\Users\downl\AppData\Local\Microsoft\WinGet\Packages\ggml.llamacpp_Microsoft.Winget.Source_8wekyb3d8bbwe`
4. 置換前ファイルは `.bak.<timestamp>` でバックアップしてから差し替え:
   - `llama-cli.exe`
   - `llama-server.exe`
   - `llama.dll`
   - `ggml.dll`
   - `ggml-base.dll`
   - `ggml-cpu.dll`
   - `ggml-cuda.dll`
   - `mtmd.dll`

## 検証結果

- `where.exe llama-cli` / `where.exe llama-server` で置換先パスを確認。
- `llama-cli --version` と `llama-server --version` が正常応答。
- 応答バージョン:
  - `version: 8483 (abe37ec28)`
  - `built with MSVC 19.50.35727.0 for x64`
- 起動時に CUDA デバイス検出ログが出力され、CUDA バックエンドが有効であることを確認。

## 補足

- MCP サーバーでの日時取得を試行したが、対象サーバーが `Not connected` で取得不可だったため、実装ログは作業日の実日付で記録した。
